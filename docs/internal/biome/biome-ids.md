# Biome ID Reference

> **Module:** `Topo.Biome.Name` (120 LOC)
> **Status:** Stub

## Overview

Complete mapping from `BiomeId` (integer codes 0–60) to human-readable
display names. The `biomeDisplayName` function is total — it handles
every constructor.

## Biome Table

<!-- TODO: Generate from Topo.Biome.Name source -->

| Code | Pattern Synonym | Display Name |
|------|----------------|--------------|
| 0 | `BiomeOcean` | Ocean |
| 1 | `BiomeDeepOcean` | Deep Ocean |
| ... | ... | ... |

See `Topo.Types` for the full list of `BiomeId` pattern synonyms.
